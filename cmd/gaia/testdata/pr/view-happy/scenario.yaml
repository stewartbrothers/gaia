description: |
  `gaia pr view 42` happy path. No --with-ci, so no /status call.

mocks:
  - method: GET
    path: /repos/o/r/pulls/42
    status: 200
    body: |
      {
        "number": 42, "title": "make it better", "state": "open",
        "user": {"login": "alice"},
        "head": {"ref": "feature/x", "sha": "deadbeef", "repo": {"full_name": "o/r"}},
        "base": {"ref": "main",      "sha": "cafebabe", "repo": {"full_name": "o/r"}},
        "merged": false,
        "body": "describe the change",
        "created_at": "2026-04-01T00:00:00Z",
        "updated_at": "2026-04-02T00:00:00Z"
      }

stages:
  - args:
      - --provider
      - forgejo
      - --api-url
      - ${apiURL}
      - --repo
      - o/r
      - pr
      - view
      - "42"
    env:
      FORGEJO_TOKEN: TEST
    exit_code: 0
